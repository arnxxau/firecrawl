apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "firecrawl.name" . }}-playwright
  labels:
    app: {{ include "firecrawl.name" . }}-playwright
spec:
  replicas: {{ .Values.playwright.replicaCount | default 1 }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: {{ include "firecrawl.name" . }}-playwright
  template:
    metadata:
      labels:
        app: {{ include "firecrawl.name" . }}-playwright
    spec:
      {{- if .Values.dockerSecretEnabled }}
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
        - name: playwright-service
          image: "{{ .Values.playwright.image.repository }}:{{ .Values.playwright.image.tag }}"
          imagePullPolicy: {{ .Values.playwright.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.playwright.port }}
          envFrom:
            - configMapRef:
                name: {{ include "firecrawl.name" . }}-playwright-config
          {{- if .Values.playwright.resources }}
          resources:
            {{- toYaml .Values.playwright.resources | nindent 12 }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.service.playwright.port }}
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.service.playwright.port }}
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
