apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "firecrawl.name" . }}-worker
  labels:
    app: {{ include "firecrawl.name" . }}-worker
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: {{ include "firecrawl.name" . }}-worker
  template:
    metadata:
      labels:
        app: {{ include "firecrawl.name" . }}-worker
    spec:
      {{- if .Values.image.dockerSecretEnabled }}
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
        - name: worker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: [ "node", "--import", "./dist/src/otel.js", "dist/src/services/queue-worker.js" ]
          env:
            - name: FLY_PROCESS_GROUP
              value: "worker"
          envFrom:
            - configMapRef:
                name: {{ include "firecrawl.name" . }}-config
            - secretRef:
                name: {{ include "firecrawl.name" . }}-secret
